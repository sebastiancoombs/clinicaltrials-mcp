{
  "name": "clinicaltrials-mcp",
  "version": "0.1.0",
  "description": "x402 MCP for ClinicalTrials.gov v2 (study search, study by NCT id, sponsor pipeline aggregation). Pay-per-call USDC on Base — no signup, no API key.",
  "private": true,
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "wrangler:dev": "wrangler dev",
    "wrangler:deploy": "wrangler deploy",
    "wrangler:dry-run": "wrangler deploy --dry-run --outdir=.wrangler-dryrun"
  },
  "dependencies": {
    "@coinbase/x402": "^2.1.0",
    "hono": "^4.6.0",
    "x402-hono": "^1.2.0"
  }
}
